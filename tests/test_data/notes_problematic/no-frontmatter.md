# Document Without Frontmatter

This document has no YAML frontmatter.

It should still be processable, with auto-generated ID and language detection.

Some German text: Das ist ein deutscher Text um die Spracherkennung zu testen.
